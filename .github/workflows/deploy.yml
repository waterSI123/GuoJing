name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ] # 监听 main 分支的变动

permissions:
  contents: write # 👈 必须有这个写权限，否则无法发布

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Install and Build 🔧
        run: |
          npm install
          npm run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist # Vite 默认打包输出目录是 dist
          branch: gh-pages # 将打包好的代码放到 gh-pages 分支
